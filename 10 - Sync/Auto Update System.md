# Auto Update System

Last refreshed: `2026-05-10`

## Current truth

The old March notes about automatic vault updates through a GCP `nip.io` webhook are stale.

What is verified now:

- the `Obsidian Updater` workflow still exists and is active in `n8n`
- the old documented webhook URLs should not be treated as current
- this vault was manually refreshed on 2026-05-10 to re-establish a trustworthy baseline

## Current operating rule

Until the update path is re-audited post-AWS migration:

- prefer direct git-backed edits to this repo
- do not assume background sync is keeping these notes fresh
- re-verify the updater workflow before relying on it again
