# Builder - Web App

Last refreshed: `2026-05-10`

## Current status

| Field | Value |
|-------|-------|
| Workflow name | `Builder - Web App` |
| Workflow ID | `xiYFZhlToYLX9g4J` |
| Active | Yes |

## Current interpretation

This workflow was previously documented as broken and inactive. That is no longer true.

What is verified now:

- it is active in the live AWS `n8n` database,
- it sits inside the active builder family,
- the old Azure-era implementation notes should not be treated as current.

## Operational note

Because the active workflow list changed materially after the old March notes, any real work on this builder should start from the live workflow JSON in `n8n`, not from the historical vault copy.
